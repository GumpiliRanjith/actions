name: ranjith
on : push
jobs:
    ranjith:
        runs-on: ubuntu-latest
        steps:
            - name: welcome
              run: |
                echo "hello"
                echo "hello " > welcome.txt
            - name: hi
              uses: actions/upload-artifact@v4
              with:
                path: welcome.txt
    kumer:
        needs: ranjith
        runs-on: ubuntu-latest
        steps:
            - name: use artifact
              uses: actions/download-artifact@v4
              with:
                path: .
            - name: list all files
              run: |
                 ls
                 pwd
                 whoami

    april:
        needs: ranjith
        runs-on: ubuntu-latest
        steps:
            - name: just
              run: echo "hello world"
    june:
        runs-on: ubuntu-latest
        needs: april
        steps:
            - name: matrix
              run: echo "just architecture"

        
            
            